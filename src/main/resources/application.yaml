server:
  port: 9010

spring:
  application:
    name: haikAI
    title: haikAI
    version: 0.0.1-SNAPSHOT
  banner:
    location: banner.txt
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-5
          temperature: 1